// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("http_forwarder", "for http_forwarder support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("http_forwarder", "enable http_forwarder support", "no");

if (PHP_HTTP_FORWARDER != "no") {
	EXTENSION("http_forwarder", "http_forwarder.c");
}

